@AGENTS.md

# 프로젝트 규칙서 (CLAUDE.md)

## 1. 프로젝트 개요
- 이름: HR 리크루팅 봇
- 목적: 제이시스메디칼 채용팀(채용담당자 4명)의 채용 업무(JD 생성, 이력서 스크리닝, 면접 일정 조율, 처우 산정, 채용 현황 대시보드, 수습평가 메일 자동화)를 지원
- 기획 상세 내용은 [PRD.md](./PRD.md)를 기준으로 한다. 기능 범위·규칙이 PRD.md와 다르게 구현되어야 한다면, 먼저 PRD.md를 함께 수정한 뒤 코드를 작업한다.

## 2. 기술 스택
- Next.js 16 (App Router) — 화면과 서버 API를 한 프로젝트에서 처리
- React 19, TypeScript
- Tailwind CSS 4 (스타일은 Tailwind 유틸리티 클래스를 우선 사용, 별도 CSS 파일 추가는 최소화)
- ESLint (`npm run lint`로 검사)
- 배포: Vercel
- 기술 스택은 PRD.md에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다.

## 3. 폴더 구조
- `app/` — 페이지·라우트·API 라우트 (App Router 규칙을 따름)
- `public/` — 정적 파일(이미지 등)
- `PRD.md` — 서비스 기획서 (기능 범위와 우선순위의 기준 문서)
- `CLAUDE.md` — 이 프로젝트 규칙서
- `AGENTS.md` — Next.js가 자동으로 관리하는 프레임워크 가이드 (직접 수정하지 않음)

## 4. AI(코드 작성 시) 지킬 규칙
- 채용 전형의 최종 합격/불합격 여부를 코드가 자동으로 확정하지 않는다. AI/시스템은 분류·추천까지만 하고, 최종 판단은 항상 담당자가 내리도록 구현한다.
- 이력서 1차 스크리닝에서 기준 충족 여부가 애매하면 "확인필요"로 분류하고 담당자에게 넘긴다 (임의로 "적합/부적합"을 단정하지 않는다).
- 면접 일정은 지원자·현업 양쪽에서 각각 최소 3개 이상의 가능 시간대를 받기 전에는 자동으로 확정하지 않으며, 확정 전 담당자 승인을 거치는 흐름으로 구현한다.
- PRD.md 6) 비범위에 명시된 기능(최종 합격 자동 확정, 실제 급여 지급·전자결재 연동, 외부 채용 플랫폼 자동 연동, 타 부서 기능 확장, 모바일 앱)은 구현하지 않는다.

## 5. 보안·개인정보 규칙
- API 키, 비밀번호 등 민감한 값은 절대 코드에 하드코딩하지 않는다. `.env` / `.env.local` 등 환경변수 파일에만 저장한다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다 (커밋 전 `git status`로 반드시 확인).
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 지원자 이력서, 연락처, 연봉 등 개인정보를 다루는 코드를 작성할 때는 목적(채용 절차) 외 용도로 로그에 남기거나 외부로 전송하지 않는다.
- 콘솔 로그, 에러 메시지 등에 지원자 개인정보나 API 키가 노출되지 않도록 주의한다.

## 6. 코딩 컨벤션
- 모든 설명과 주석은 한국어로 작성한다.
- 컴포넌트는 함수형 컴포넌트 + TypeScript로 작성한다.
- 의미가 명확한 변수·함수명을 사용하고, 불필요한 주석은 넣지 않는다.
- 코드 작성 후 `npm run lint`로 검사하고, 가능하면 `npm run build`로 빌드 오류가 없는지 확인한다.

## 7. 작업 방식 규칙
- 새 파일은 `my-app` 폴더 안에만 만든다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.
