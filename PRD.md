# HR 리크루팅 봇 서비스 기획서 (PRD)

- 작성자: HR팀
- 작성일: 2026-09-14

## 1) 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

제이시스메디칼 채용팀(채용담당자 4명)은 현업 부서가 상신한 채용요청서를 공고(JD)로 변환하는 작업, 지원자 이력서를 HR 기준으로 1차 스크리닝하는 작업, 지원자와 현업 간 면접 일정을 조율하는 작업, 합격자의 처우(연봉)를 직전연봉 기준으로 산정하는 작업, 그리고 채용 현황을 파악할 수 있는 대시보드를 관리하는 작업까지 채용 전 과정을 수작업으로 처리하고 있다. 이 과정에서 반복적인 문서 작업과 다자간 일정 조율에 많은 시간이 소요되어, 정작 후보자 검증과 같은 핵심 업무에 집중하기 어려운 불편을 겪고 있다.

## 2) 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

현재 채용팀은 현업의 채용요청서를 참고해 JD를 담당자가 직접 워드/한글 문서로 작성하고, 지원자 이력서는 담당자가 하나씩 열람하며 HR 기준(경력, 자격요건 등)에 맞는지 수기로 검토한다. 면접 일정은 이메일·메신저·전화로 지원자와 현업의 가능 시간을 여러 차례 주고받으며 맞춘다. 처우(연봉) 산정은 지원자가 제출한 경력증명서·원천징수영수증 등을 담당자가 직접 확인해 직전연봉을 파악한 뒤 산정한다. 채용 현황은 담당자별로 엑셀 시트를 개별 관리하고 있어, 전체 현황을 한눈에 보려면 별도로 취합하는 과정이 필요하다. 이처럼 각 단계가 담당자의 수작업에 의존하다 보니 처리 속도가 느리고, 담당자 간 기준이 조금씩 달라질 수 있으며, 현황 파악에도 시간이 걸린다.

## 3) 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- JD 작성 시간: 채용요청서 접수 후 JD 초안 완성까지 평균 소요시간을 (예: 1~2일 → 1시간 이내)로 단축
- 이력서 1차 스크리닝: 지원자 1인당 검토 시간을 (예: 10분 → 2분 이내)로 단축하고, 전체 지원자 중 HR 기준 적합자를 자동 분류
- 면접 일정 조율: 지원자·현업 간 일정 확정까지 걸리는 소요일을 (예: 평균 3~5일 → 1~2일)로 단축
- 처우(연봉) 산정: 직전연봉 확인 및 산정 소요시간을 (예: 30분 → 5분 이내)로 단축, 산정 오류(재확인 요청) 건수 0건 목표
- 채용 현황 파악: 담당자 4명의 현황을 실시간 대시보드 1곳에서 확인, 별도 취합 작업 0건화
- 수습평가 메일 자동화: 신규입사자(수습 3개월 이내) 대상으로 입사일 기준 45일 차 중간평가, 80일 차 최종평가 안내 메일을 담당자가 매번 대상자를 확인해 수기로 작성·발송하는 대신, 입사일이 등록되면 시스템이 대상자와 발송 시점을 자동 산출해 정해진 템플릿 메일을 자동 발송하도록 함. 건당 작성·발송 소요시간을 (예: 15분 → 0분, 완전 자동화)로 줄이고, 발송 누락·지연 건수 0건을 목표로 함

## 4) 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

**주 사용자**: 제이시스메디칼 채용담당자 4명 (현업 부서 담당자·지원자는 메일/알림으로 간접 참여)

**이용 흐름**
1. 현업 부서 채용요청서 상신 → 채용담당자가 봇에 요청서 입력 → 봇이 JD 초안 생성 → 담당자 검토·수정 후 공고 게시
2. 지원자 이력서 접수 → 봇이 HR 기준으로 1차 스크리닝(적합/부적합 분류) → 담당자가 결과 확인 후 다음 단계(서류합격) 진행
3. 서류합격자 확정 → 담당자가 봇에 면접 후보 일정 입력 → 봇이 지원자·현업 가능 시간 조율 → 확정된 일정 자동 공유(담당자·현업·지원자)
4. 최종합격자 확정 → 담당자가 봇에 지원자 경력 정보 입력 → 봇이 직전연봉 기준 처우(안) 산출 → 담당자 검토 후 확정
5. 신규입사자 등록(입사일 입력) → 봇이 45일 차·80일 차 시점을 자동 산출 → 해당 시점에 평가 안내 메일 자동 발송(담당자는 결과만 확인)
6. 담당자는 대시보드에서 위 1~5 전체 채용·수습 현황을 실시간으로 한눈에 확인

## 5) 주요 기능 (must / nice로 구분)
*must는 딱 2개까지, 기능마다 "AI가 지킬 규칙"을 함께*

**[Must]**
1. **이력서 1차 스크리닝**
   - AI가 지킬 규칙: HR이 사전 설정한 필수 자격요건(최소 경력 연차, 필수 자격증·전공 등)을 충족하지 못하면 자동으로 "부적합"으로 분류한다. 충족 여부가 애매한 항목이 1개라도 있으면 "확인필요"로 표시해 담당자에게 넘기며, AI가 임의로 "최종 합격/불합격"을 확정하지 않는다. "확인필요"로 분류된 지원자는 담당자가 이력서를 직접 검토해 최종 판단한다.
2. **면접 일정 조율**
   - AI가 지킬 규칙: 지원자·현업 양쪽에서 각각 최소 3개 이상의 가능 시간대를 받기 전에는 일정을 확정 제안하지 않는다. 확정 전 반드시 담당자의 최종 승인을 거치며, 조율 요청 후 48시간 내 회신이 없으면 담당자에게 알림을 보낸다. 지원자 또는 현업이 제시한 가능 시간대가 3개 미만이면 자동 조율을 진행하지 않고 담당자에게 즉시 알림을 보내 직접 조율하도록 한다.

**[Nice]**
3. JD 자동 생성 — 채용요청서 기반 초안 생성, 최종 게시 전 담당자 검토 필수
4. 처우(연봉) 산정 — 직전연봉 기반 처우(안) 자동 산출, 최종 확정은 담당자 승인 필요
5. 채용 현황 대시보드 — 1~4 전체 현황 실시간 집계·시각화
6. 수습평가 메일 자동화 — 입사일 기준 45일/80일 차 평가 메일 자동 발송, 발송 전 최종 1회 담당자 확인(선택) 옵션 제공

## 6) 범위와 비범위
*이번 실습에서 만들 것 / 절대 만들지 않을 것*

**범위 (이번에 만들 것)**
- JD 초안 자동 생성
- 지원자 이력서 1차 스크리닝 (적합/부적합/확인필요 분류)
- 지원자-현업 면접 일정 조율 지원
- 직전연봉 기준 처우(안) 산출 보조
- 채용 현황 대시보드
- 신규입사자 수습평가(45일/80일 차) 메일 자동 발송
- 사내 ATS(나인하이어) **읽기 전용** 연동 — 채용 공고 정보 조회부터 시작하며, 지원자 개인정보 조회는 로그인·접근 제어를 붙인 뒤에 진행한다

**비범위 (절대 만들지 않을 것)**
- 최종 합격/불합격 여부를 AI가 자동으로 확정하는 기능 (모든 최종 결정은 담당자가 함)
- 실제 급여 지급, 전자결재·인사시스템(예: 그룹웨어, ERP) 연동
- 잡코리아·사람인 등 외부 구직 플랫폼에 공고를 자동으로 게시하거나 이력서를 자동 수집하는 기능 (사내에서 사용 중인 나인하이어 ATS 읽기 전용 연동은 예외로 범위에 포함)
- 나인하이어에 데이터를 생성·수정·삭제하는 쓰기 연동 (조회만 수행한다)
- 채용팀 외 타 부서(재무, 총무 등) 업무로의 기능 확장
- 모바일 앱 형태 개발 (우선 웹 기반으로만 진행)

## 7) 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- **공개등급**: 사내 기밀 문서로 분류. 채용담당자 4명만 접근 가능하며, 외부 공유·반출 금지.
- **로그인·접근 제어**: Supabase 인증(이메일+비밀번호)으로 로그인한 사용자만 모든 화면과 API를 사용할 수 있다. 공개 가입은 차단하고, 계정은 관리자가 직접 생성한다.
- **역할별 권한**: 계정은 `관리자(admin)`와 `일반 사용자(user, 채용담당자)` 두 역할로 나눈다. 구분 기준은 **"지원자(후보자) 데이터는 담당자 모두 / 재직자(직원) 데이터와 시스템 설정은 관리자만"** 이다.
  - 두 역할 공통: 대시보드 채용 진행 현황, 이력서 스크리닝, 면접 일정 조율, 처우안 산정(서류 분석·연봉 계산·엑셀 생성)
  - 관리자 전용 ①: **인사마스터 벤치마크 조회** — 지원자가 아닌 전 직원의 급여 정보이며 주민등록번호·계좌번호가 포함된 파일을 다루므로 접근을 최소화한다
  - 관리자 전용 ②: **ATS(나인하이어) 연동 화면 및 공고 조회 API** — 연동 설정과 API 키 상태를 다루는 관리 기능
  - 관리자 전용 ③: **대시보드의 수습평가 대상자 정보** — 지원자가 아닌 재직 직원의 이름·부서·입사일
  - 권한 확인은 화면 숨김만으로 처리하지 않고, 페이지와 API 양쪽에서 서버가 직접 검사한다 (주소 직접 입력·API 직접 호출로 우회 불가).
- **개인정보 여부**: 지원자 이력서(이름, 연락처, 학력, 경력 등)와 합격자 처우(연봉) 정보 등 개인정보보호법상 민감한 개인정보를 다룸. 수집 목적(채용 절차) 외 용도로 사용하지 않으며, 채용 전형 종료 후 1년이 지나면 파기하는 것을 원칙으로 함.
- **ATS 연동 시 주의**: 나인하이어 API는 워크스페이스 단위 API 키로 인증하므로, 키는 서버에서만 사용하고 브라우저(클라이언트)로 절대 전달하지 않는다. 지원자 개인정보를 조회하는 연동은 로그인·접근 제어가 적용된 뒤에 진행한다.
- **API 키 관리**: AI 연동 및 나인하이어 연동에 사용하는 API 키는 소스코드에 직접 넣지 않고 환경변수 파일(`.env.local`)에 저장하며, 이 파일은 `.gitignore`에 등록해 Git 저장소에 올라가지 않도록 함. Vercel 배포 시에는 Vercel 프로젝트의 Environment Variables 설정에 등록해 관리하고, 키가 노출된 경우 즉시 재발급한다.

## 8) 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- **Next.js**: 화면(프론트엔드)과 서버 쪽 코드(API)를 한 프로젝트에서 함께 처리
- **고정 이유**: STEP 2에서 `my-app`에 이미 설치했고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함
